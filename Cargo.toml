[package]
name = "tiny-metal-llm"
version = "0.1.0"
edition = "2024"

[dependencies]
metal = "0.33.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

safetensors = "0.8"
half = "2"

clap = {
    version = "4",
    features = ["derive"]
}
sentencepiece = "0.14"
